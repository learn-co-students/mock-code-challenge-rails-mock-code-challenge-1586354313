 <%= form_for @signup do |f| %>
  <%= f.time_select :time %>
  <%= f.collection_select :camper, Camper.all, :id, :name %>
  <%= f.collection_select :activity, Activity.all, :id, :name %>
  <%= f.submit %>
<% end %>